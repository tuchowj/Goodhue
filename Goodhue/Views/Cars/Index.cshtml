@using Microsoft.AspNet.Identity
@model IEnumerable<Goodhue.Models.Car>

@{
    ViewBag.Title = "Home";
}

<div class="jumbotron">
    <h1>Goodhue County Car Share</h1>
</div>

<div class="row">
    <form action="" method="post">
        <div class="col-sm-3"><h2>Find Available Car:</h2></div>
        <div class="col-sm-3"><strong>Start Date</strong><input class="form-control text-box single-line" data-val="true" data-val-required="The Start Date field is required." id="StartDate" name="StartDate" type="date" value=""></div>
        <div class="col-sm-3"><strong>End Date</strong><input class="form-control text-box single-line" data-val="true" data-val-required="The End Date field is required." id="EndDate" name="EndDate" type="date" value=""></div>
        <div class="col-sm-1"><br /><input class="btn btn-default" type="submit" value="Search" /></div>
    </form>
</div>
@if (User.Identity.GetUserName() == ViewBag.Admin)
{
    <p>
        @Html.ActionLink("Create New", "Create", "Cars")
    </p>
}
<table class="table sortable">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CountyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        @if (User.Identity.GetUserName() == ViewBag.Admin)
        {
            <th>
                @Html.DisplayNameFor(model => model.Odometer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OilChangeMiles)
            </th>
        }
        <th>
            @Html.DisplayNameFor(model => model.LastReservation)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        if (item.IsAvailable)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CountyID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Location)
                </td>
                @if (User.Identity.GetUserName() == ViewBag.Admin)
                {
                    <td>
                        @Html.DisplayFor(modelItem => item.Odometer)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OilChangeMiles)
                    </td>
                }
                <td>
                    @Html.DisplayFor(modelItem => item.LastReservation)
                </td>
                <td>
                    @Html.ActionLink("Select", "Schedule", "Reservations", new { id = item.ID }, null)
                    @*@Html.ActionLink("Details", "Details", "Cars", new { id = item.ID }, null) |*@
                    
                    @if (User.Identity.GetUserName() == ViewBag.Admin)
                    {
                        <span>|</span>
                        @Html.ActionLink("Edit", "Edit", "Cars", new { id = item.ID }, null)
                    
                    }
                
                    @if (User.Identity.GetUserName() == ViewBag.Admin)
                    {
                        <span>|</span>
                        @Html.ActionLink("Delete", "Delete", "Cars", new { id = item.ID }, null)
                    }
                </td>
            </tr>
        }
    }

</table>
@if (User.Identity.GetUserName() == ViewBag.Admin)
{
    <h2>Unavailable Cars</h2>
    <table class="table sortable">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountyID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Odometer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OilChangeMiles)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastReservation)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            if (!item.IsAvailable)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CountyID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Location)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Odometer)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OilChangeMiles)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastReservation)
                    </td>
                    <td>
                        @*@Html.ActionLink("Schedule", "Schedule", "Reservations", new { id = item.ID }, null) |*@
                        @Html.ActionLink("Details", "Details", "Cars", new { id = item.ID }, null)
                        @*@Html.ActionLink("Checkout", "Create", "Reservations", new { id = item.ID }, null)*@
                        @if (User.Identity.GetUserName() == ViewBag.Admin)
                        {
                            <span>|</span>
                            @Html.ActionLink("Edit", "Edit", "Cars", new { id = item.ID }, null)

                        }

                        @if (User.Identity.GetUserName() == ViewBag.Admin)
                        {
                            <span>|</span>
                            @Html.ActionLink("Delete", "Delete", "Cars", new { id = item.ID }, null)
                        }
                    </td>
                </tr>
            }
        }

    </table>
}